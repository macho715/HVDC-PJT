# Minimal HVDC Project Dependencies - Optimized for Performance
# Core Python packages only - 60-70% smaller than full requirements

# Essential data processing (lightweight alternatives)
polars>=0.20.0          # Fast DataFrame library (alternative to pandas)

# Core utilities
pyyaml>=6.0            # YAML processing
requests>=2.28.0       # HTTP requests
python-dotenv>=1.0.0   # Environment variables

# Database
sqlite3                # Built-in Python module

# Optional performance enhancements (install only if needed)
# pandas>=1.5.0        # Heavy data processing (optional)
# numpy>=1.24.0        # Numerical computing (optional)
# plotly>=5.0.0        # Visualization (optional)
# scikit-learn>=1.3.0  # Machine learning (optional)
# rdflib>=6.0.0        # RDF processing (optional)

# Development and monitoring
psutil>=5.9.0          # System monitoring
memory-profiler>=0.60.0 # Memory profiling (dev only)

# Production optimizations
gunicorn>=20.1.0       # WSGI server
uvloop>=0.17.0         # Fast event loop (Unix only)